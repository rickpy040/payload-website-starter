import type { Block } from 'payload'

/** POI page header: title, intro, and the prominent back-link to the primary location. */
export const PoiHero: Block = {
  slug: 'poiHero',
  interfaceName: 'PoiHeroBlock',
  labels: { singular: 'Kop', plural: 'Koppen' },
  fields: [],
}
